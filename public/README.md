# Public Assets

Place static assets here to be served at the root URL.
To replace the hero portrait, upload your image file directly to this folder via the file explorer in Google AI Studio and name it `portrait.png`.
